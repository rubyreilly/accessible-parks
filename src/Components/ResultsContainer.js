import React, { Component } from 'react';

class ResultsContainer extends Component{

  render(){
    return(
      <div>ResultsContainer (generate result cards)</div>
    )
  }
}

export default ResultsContainer
